---
{"dg-publish":true,"permalink":"//2///bs-assignment-4/"}
---

<script>
MathJax = {
  tex: {
    inlineMath: [[',
    displayMath: [['$', '$'], ['\\[', '\\]'\|'$', '$'], ['\\[', '\\]']],
    processEscapes: true,
  },
  svg: {
    fontCache: 'global'
  }
};
</script>
<script type="text/javascript" id="MathJax-script" async
  src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-svg.js">
</script>

>[!QUOTE] 과제 4
>본 노트는 **의학통계학 1차 실습** 과제 4번의 답안에 관한 노트입니다!
>
>수식 및 분석 노트를 원활하게 작성하기 위해 **마크다운 앱**에서 진행했습니다!
>
>PPT 단일로는 **같은 마크다운 문법을 사용하는 생성형 AI와 혼동될 여지**을 고려하여 이렇게 노트의 링크도 첨부했습니다.

---

>[!CHECK] 참여자
>**송채은, 이시연, 정우진**이 본 노트 작성에 참여했습니다!

---

## 1. 다중회귀분석 실시 및 회귀식 도출 (과제 4-1)

> [!info] **분석 설정**
> - **종속변수**: `riskScoreAD` 
> - **설명변수**: `wmhTotalVol`, `PeriWMHVol`, `rh_volWM`, `rh_wVolHippocampus` 
> - **분석 방법**: 입력(Enter) 방식 

### 분석 결과 요약

- **회귀 계수 및 유의성**:
    - `PeriWMHVol` ($B=0.011, p=0.008$), `rh_volWM` ($B=0.000, p<.001$), `rh_wVolHippocampus` ($B=-8.426, p<.001$)는 유의미한 영향력을 보였습니다. 
    - 반면, `wmhTotalVol` ($B=-0.006, p=0.129$)은 통계적으로 유의하지 않았습니다. 

### 회귀식 (Regression Equation)
$$Y = 88.141 - 0.006 \times \text{wmhTotalVol} + 0.011 \times \text{PeriWMHVol} + 0.000 \times \text{rh\_volWM} - 8.426 \times \text{rh\_wVolHippocampus}$$ 


---

## 2. 다중공선성 진단 (과제 4-2)

- **VIF(분산팽창요인) 결과**:
    - `wmhTotalVol`: **30.954** 
    - `PeriWMHVol`: **30.696** 
- **진단**: 일반적으로 VIF가 10 이상이면 다중공선성이 존재한다고 판단합니다.  본 모델에서는 `wmhTotalVol`과 `PeriWMHVol` 간에 매우 높은 공선성이 발견됐습니다.

---

## 3. 모델의 설명력 및 유의성 (과제 4-3)

- **수정된 결정계수 (Adjusted $R^2$)**: **0.284** 
    - 이는 모델에 포함된 변수들이 `riskScoreAD` 변동의 약 28.4%를 설명하고 있음을 의미합니다. 
- **모델 유의성 (ANOVA)**: $F=28.008, p<.001$ 
    - 유의확률이 0.05 미만이므로, 이 회귀 모델은 통계적으로 매우 유의하며 종속변수를 예측하기에 적합합니다. 

---

## 4. 명목변수 가변수 처리 및 분석 (과제 4-4)


### A. 입력 방식 (Enter Method) 분석 결과

> [!info] 입력 방식 (Enter Method)
> 모든 독립변수를 동시에 투입하여 가변수의 개별 기여도를 평가함.

### **통계 결과** 
| 가변수        | 비표준화 계수(B) | 유의확률(p)  | 결과 해석                |
| :--------- | :--------: | :------: | :------------------- |
| **Look_1** |   9.445    | **.050** | 통계적 유의성 **경계선상**에 있음 |
| **Look_2** |   2.736    | **.763** | 통계적으로 유의하지 **않음**    |

### **관계 해석**

- **Look_1과 종속변수의 관계**: `Look_1`의 **p-값이 0.050**으로 나타나, 유의수준 5%에서 
  종속변수인 `riskScoreAD`와 통계적으로 유의한 관련성이 있다기엔 **경계선**에 있습니다. 
  다만, 양(+)의 계수 값을 가짐에 따라 참조 집단에 비해 위험 점수가 다소 높은 경향을 보입니다.

- **Look_2와 종속변수의 관계**: p-값이 0.763으로 매우 높아, `Look_2` 특성은 `riskScoreAD` 점수 변화에 통계적으로 아무런 영향을 미치지 않는 것으로 판단됩니다.

---

### B. 단계 선택 방식 (Stepwise Method) 분석 결과

> [!info] 단계 선택 방식 (Stepwise Method)
> 통계적으로 유의미한 변수만을 선별하여 최적의 모델을 구축함.

### **통계 결과** 
| 가변수 | 비표준화 계수(B) | 유의확률(p) | 결과 해석 |
| :--- | :---: | :---: | :--- |
| **Look_1** | 8.710 | **.026** | **통계적으로 유의함** (최종 모델 포함) |
| **Look_2** | - | - | **제외됨** (유의성 부족) |

### **관계 해석**
- **Look_1과 종속변수의 관계**: 단계 선택법의 최종 모델(4단계)에서 **`Look_1`은 p-값 0.026**으로 통계적으로 **유의한 변수**로 채택되었습니다. 이는 다른 유의한 변수(`rh_wVolHippocampus` 등)들의 영향력을 통제했을 때, **`Look_1` 집단이 참조 집단보다 `riskScoreAD` 점수가 약 8.710점 더 높음**을 의미합니다.
- **Look_2와 종속변수의 관계**: `Look_2`는 변수 선택 과정에서 **유의확률이 낮아** 모델에 진입하지 못하고 제외되었습니다. 이는 `Look_2`가 종속변수를 설명하는 데 통계적 기여도가 없음을 시사합니다.

---

### C. 종합 결론 및 비교

> [!check] **핵심 요약**
> - 1. 본 4-4 과제에선, `LOOK`의 의미를 해석할 수 없어, 의학적으로 **유의미한 변수인지 아닌지**를 알 수 없기 때문에 일반적으로 사용하는 **입력**방식과 통계적인 값을 중점적으로 고려하는 **단계 선택** 방식을 모두 사용했습니다.
>      
> - 2. **입력 방식**에서는 `Look_1`의 유의성이 모호했으나, 불필요한 변수(유의하지 않은 변수들)를 제거한 **단계 선택 방식**에서는 `Look_1`이 종속변수에 유의한 영향을 미치는 핵심 변수임이 명확히 드러났습니다.
>      
> - 3. 반면, **`Look_2`**는 어떤 분석 방식에서도 종속변수와 유의미한 관계를 맺지 못하는 것으로 확인되었습니다.
---

---
## 5. 후진 제거법을 통한 최적 모델 구축 (과제 4-5)

> [!rocket] **단계적 변수 선택 (Backward Method)**
> 유의하지 않은 변수를 하나씩 제거하여 가장 최적화된 모델을 도출했습니다. 

### 모델 구축 과정

1. **1단계**: 모든 변수 포함. 
2. **2단계**: 가장 유의하지 않은 `Look_2`($p=0.763$) 제거. 
3. **3단계**: 다음으로 유의하지 않은 `wmhTotalVol`($p=0.134$) 제거. 

### 최종 모델 결과 (모형 3)

- **최종 선택 변수**: `PeriWMHVol`, `rh_volWM`, `rh_wVolHippocampus`, `Look_1` 
- **최종 모델 적합도**: 수정된 $R^2 = 0.288$, ANOVA $p<.001$ 
- **최종 해석**: 변수를 제거하기 전(0.284)보다 모델의 설명력이 향상(0.288)되었으며, 다중공선성 문제도 완화된 가장 효율적인 모델이 구축되었습니다. 

---
, ',
    displayMath: [['$$', '$$'], ['\\[', '\\]']],
    processEscapes: true,
  },
  svg: {
    fontCache: 'global'
  }
};
</script>
<script type="text/javascript" id="MathJax-script" async
  src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-svg.js">
</script>

>[!QUOTE] 과제 4
>본 노트는 **의학통계학 1차 실습** 과제 4번의 답안에 관한 노트입니다!
>
>수식 및 분석 노트를 원활하게 작성하기 위해 **마크다운 앱**에서 진행했습니다!
>
>PPT 단일로는 **같은 마크다운 문법을 사용하는 생성형 AI와 혼동될 여지**을 고려하여 이렇게 노트의 링크도 첨부했습니다.

---

>[!CHECK] 참여자
>**송채은, 이시연, 정우진**이 본 노트 작성에 참여했습니다!

---

## 1. 다중회귀분석 실시 및 회귀식 도출 (과제 4-1)

> [!info] **분석 설정**
> - **종속변수**: `riskScoreAD` 
> - **설명변수**: `wmhTotalVol`, `PeriWMHVol`, `rh_volWM`, `rh_wVolHippocampus` 
> - **분석 방법**: 입력(Enter) 방식 

### 분석 결과 요약

- **회귀 계수 및 유의성**:
    - `PeriWMHVol` ($B=0.011, p=0.008$), `rh_volWM` ($B=0.000, p<.001$), `rh_wVolHippocampus` ($B=-8.426, p<.001$)는 유의미한 영향력을 보였습니다. 
    - 반면, `wmhTotalVol` ($B=-0.006, p=0.129$)은 통계적으로 유의하지 않았습니다. 

### 회귀식 (Regression Equation)
$$Y = 88.141 - 0.006 \times \text{wmhTotalVol} + 0.011 \times \text{PeriWMHVol} + 0.000 \times \text{rh\_volWM} - 8.426 \times \text{rh\_wVolHippocampus}$$ 


---

## 2. 다중공선성 진단 (과제 4-2)

- **VIF(분산팽창요인) 결과**:
    - `wmhTotalVol`: **30.954** 
    - `PeriWMHVol`: **30.696** 
- **진단**: 일반적으로 VIF가 10 이상이면 다중공선성이 존재한다고 판단합니다.  본 모델에서는 `wmhTotalVol`과 `PeriWMHVol` 간에 매우 높은 공선성이 발견됐습니다.

---

## 3. 모델의 설명력 및 유의성 (과제 4-3)

- **수정된 결정계수 (Adjusted $R^2$)**: **0.284** 
    - 이는 모델에 포함된 변수들이 `riskScoreAD` 변동의 약 28.4%를 설명하고 있음을 의미합니다. 
- **모델 유의성 (ANOVA)**: $F=28.008, p<.001$ 
    - 유의확률이 0.05 미만이므로, 이 회귀 모델은 통계적으로 매우 유의하며 종속변수를 예측하기에 적합합니다. 

---

## 4. 명목변수 가변수 처리 및 분석 (과제 4-4)


### A. 입력 방식 (Enter Method) 분석 결과

> [!info] 입력 방식 (Enter Method)
> 모든 독립변수를 동시에 투입하여 가변수의 개별 기여도를 평가함.

### **통계 결과** 
| 가변수        | 비표준화 계수(B) | 유의확률(p)  | 결과 해석                |
| :--------- | :--------: | :------: | :------------------- |
| **Look_1** |   9.445    | **.050** | 통계적 유의성 **경계선상**에 있음 |
| **Look_2** |   2.736    | **.763** | 통계적으로 유의하지 **않음**    |

### **관계 해석**

- **Look_1과 종속변수의 관계**: `Look_1`의 **p-값이 0.050**으로 나타나, 유의수준 5%에서 
  종속변수인 `riskScoreAD`와 통계적으로 유의한 관련성이 있다기엔 **경계선**에 있습니다. 
  다만, 양(+)의 계수 값을 가짐에 따라 참조 집단에 비해 위험 점수가 다소 높은 경향을 보입니다.

- **Look_2와 종속변수의 관계**: p-값이 0.763으로 매우 높아, `Look_2` 특성은 `riskScoreAD` 점수 변화에 통계적으로 아무런 영향을 미치지 않는 것으로 판단됩니다.

---

### B. 단계 선택 방식 (Stepwise Method) 분석 결과

> [!info] 단계 선택 방식 (Stepwise Method)
> 통계적으로 유의미한 변수만을 선별하여 최적의 모델을 구축함.

### **통계 결과** 
| 가변수 | 비표준화 계수(B) | 유의확률(p) | 결과 해석 |
| :--- | :---: | :---: | :--- |
| **Look_1** | 8.710 | **.026** | **통계적으로 유의함** (최종 모델 포함) |
| **Look_2** | - | - | **제외됨** (유의성 부족) |

### **관계 해석**
- **Look_1과 종속변수의 관계**: 단계 선택법의 최종 모델(4단계)에서 **`Look_1`은 p-값 0.026**으로 통계적으로 **유의한 변수**로 채택되었습니다. 이는 다른 유의한 변수(`rh_wVolHippocampus` 등)들의 영향력을 통제했을 때, **`Look_1` 집단이 참조 집단보다 `riskScoreAD` 점수가 약 8.710점 더 높음**을 의미합니다.
- **Look_2와 종속변수의 관계**: `Look_2`는 변수 선택 과정에서 **유의확률이 낮아** 모델에 진입하지 못하고 제외되었습니다. 이는 `Look_2`가 종속변수를 설명하는 데 통계적 기여도가 없음을 시사합니다.

---

### C. 종합 결론 및 비교

> [!check] **핵심 요약**
> - 1. 본 4-4 과제에선, `LOOK`의 의미를 해석할 수 없어, 의학적으로 **유의미한 변수인지 아닌지**를 알 수 없기 때문에 일반적으로 사용하는 **입력**방식과 통계적인 값을 중점적으로 고려하는 **단계 선택** 방식을 모두 사용했습니다.
>      
> - 2. **입력 방식**에서는 `Look_1`의 유의성이 모호했으나, 불필요한 변수(유의하지 않은 변수들)를 제거한 **단계 선택 방식**에서는 `Look_1`이 종속변수에 유의한 영향을 미치는 핵심 변수임이 명확히 드러났습니다.
>      
> - 3. 반면, **`Look_2`**는 어떤 분석 방식에서도 종속변수와 유의미한 관계를 맺지 못하는 것으로 확인되었습니다.
---

---
## 5. 후진 제거법을 통한 최적 모델 구축 (과제 4-5)

> [!rocket] **단계적 변수 선택 (Backward Method)**
> 유의하지 않은 변수를 하나씩 제거하여 가장 최적화된 모델을 도출했습니다. 

### 모델 구축 과정

1. **1단계**: 모든 변수 포함. 
2. **2단계**: 가장 유의하지 않은 `Look_2`($p=0.763$) 제거. 
3. **3단계**: 다음으로 유의하지 않은 `wmhTotalVol`($p=0.134$) 제거. 

### 최종 모델 결과 (모형 3)

- **최종 선택 변수**: `PeriWMHVol`, `rh_volWM`, `rh_wVolHippocampus`, `Look_1` 
- **최종 모델 적합도**: 수정된 $R^2 = 0.288$, ANOVA $p<.001$ 
- **최종 해석**: 변수를 제거하기 전(0.284)보다 모델의 설명력이 향상(0.288)되었으며, 다중공선성 문제도 완화된 가장 효율적인 모델이 구축되었습니다. 

---
], ['\\(', '\\)'\|',
    displayMath: [['$$', '$$'], ['\\[', '\\]']],
    processEscapes: true,
  },
  svg: {
    fontCache: 'global'
  }
};
</script>
<script type="text/javascript" id="MathJax-script" async
  src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-svg.js">
</script>

>[!QUOTE] 과제 4
>본 노트는 **의학통계학 1차 실습** 과제 4번의 답안에 관한 노트입니다!
>
>수식 및 분석 노트를 원활하게 작성하기 위해 **마크다운 앱**에서 진행했습니다!
>
>PPT 단일로는 **같은 마크다운 문법을 사용하는 생성형 AI와 혼동될 여지**을 고려하여 이렇게 노트의 링크도 첨부했습니다.

---

>[!CHECK] 참여자
>**송채은, 이시연, 정우진**이 본 노트 작성에 참여했습니다!

---

## 1. 다중회귀분석 실시 및 회귀식 도출 (과제 4-1)

> [!info] **분석 설정**
> - **종속변수**: `riskScoreAD` 
> - **설명변수**: `wmhTotalVol`, `PeriWMHVol`, `rh_volWM`, `rh_wVolHippocampus` 
> - **분석 방법**: 입력(Enter) 방식 

### 분석 결과 요약

- **회귀 계수 및 유의성**:
    - `PeriWMHVol` ($B=0.011, p=0.008$), `rh_volWM` ($B=0.000, p<.001$), `rh_wVolHippocampus` ($B=-8.426, p<.001$)는 유의미한 영향력을 보였습니다. 
    - 반면, `wmhTotalVol` ($B=-0.006, p=0.129$)은 통계적으로 유의하지 않았습니다. 

### 회귀식 (Regression Equation)
$$Y = 88.141 - 0.006 \times \text{wmhTotalVol} + 0.011 \times \text{PeriWMHVol} + 0.000 \times \text{rh\_volWM} - 8.426 \times \text{rh\_wVolHippocampus}$$ 


---

## 2. 다중공선성 진단 (과제 4-2)

- **VIF(분산팽창요인) 결과**:
    - `wmhTotalVol`: **30.954** 
    - `PeriWMHVol`: **30.696** 
- **진단**: 일반적으로 VIF가 10 이상이면 다중공선성이 존재한다고 판단합니다.  본 모델에서는 `wmhTotalVol`과 `PeriWMHVol` 간에 매우 높은 공선성이 발견됐습니다.

---

## 3. 모델의 설명력 및 유의성 (과제 4-3)

- **수정된 결정계수 (Adjusted $R^2$)**: **0.284** 
    - 이는 모델에 포함된 변수들이 `riskScoreAD` 변동의 약 28.4%를 설명하고 있음을 의미합니다. 
- **모델 유의성 (ANOVA)**: $F=28.008, p<.001$ 
    - 유의확률이 0.05 미만이므로, 이 회귀 모델은 통계적으로 매우 유의하며 종속변수를 예측하기에 적합합니다. 

---

## 4. 명목변수 가변수 처리 및 분석 (과제 4-4)


### A. 입력 방식 (Enter Method) 분석 결과

> [!info] 입력 방식 (Enter Method)
> 모든 독립변수를 동시에 투입하여 가변수의 개별 기여도를 평가함.

### **통계 결과** 
| 가변수        | 비표준화 계수(B) | 유의확률(p)  | 결과 해석                |
| :--------- | :--------: | :------: | :------------------- |
| **Look_1** |   9.445    | **.050** | 통계적 유의성 **경계선상**에 있음 |
| **Look_2** |   2.736    | **.763** | 통계적으로 유의하지 **않음**    |

### **관계 해석**

- **Look_1과 종속변수의 관계**: `Look_1`의 **p-값이 0.050**으로 나타나, 유의수준 5%에서 
  종속변수인 `riskScoreAD`와 통계적으로 유의한 관련성이 있다기엔 **경계선**에 있습니다. 
  다만, 양(+)의 계수 값을 가짐에 따라 참조 집단에 비해 위험 점수가 다소 높은 경향을 보입니다.

- **Look_2와 종속변수의 관계**: p-값이 0.763으로 매우 높아, `Look_2` 특성은 `riskScoreAD` 점수 변화에 통계적으로 아무런 영향을 미치지 않는 것으로 판단됩니다.

---

### B. 단계 선택 방식 (Stepwise Method) 분석 결과

> [!info] 단계 선택 방식 (Stepwise Method)
> 통계적으로 유의미한 변수만을 선별하여 최적의 모델을 구축함.

### **통계 결과** 
| 가변수 | 비표준화 계수(B) | 유의확률(p) | 결과 해석 |
| :--- | :---: | :---: | :--- |
| **Look_1** | 8.710 | **.026** | **통계적으로 유의함** (최종 모델 포함) |
| **Look_2** | - | - | **제외됨** (유의성 부족) |

### **관계 해석**
- **Look_1과 종속변수의 관계**: 단계 선택법의 최종 모델(4단계)에서 **`Look_1`은 p-값 0.026**으로 통계적으로 **유의한 변수**로 채택되었습니다. 이는 다른 유의한 변수(`rh_wVolHippocampus` 등)들의 영향력을 통제했을 때, **`Look_1` 집단이 참조 집단보다 `riskScoreAD` 점수가 약 8.710점 더 높음**을 의미합니다.
- **Look_2와 종속변수의 관계**: `Look_2`는 변수 선택 과정에서 **유의확률이 낮아** 모델에 진입하지 못하고 제외되었습니다. 이는 `Look_2`가 종속변수를 설명하는 데 통계적 기여도가 없음을 시사합니다.

---

### C. 종합 결론 및 비교

> [!check] **핵심 요약**
> - 1. 본 4-4 과제에선, `LOOK`의 의미를 해석할 수 없어, 의학적으로 **유의미한 변수인지 아닌지**를 알 수 없기 때문에 일반적으로 사용하는 **입력**방식과 통계적인 값을 중점적으로 고려하는 **단계 선택** 방식을 모두 사용했습니다.
>      
> - 2. **입력 방식**에서는 `Look_1`의 유의성이 모호했으나, 불필요한 변수(유의하지 않은 변수들)를 제거한 **단계 선택 방식**에서는 `Look_1`이 종속변수에 유의한 영향을 미치는 핵심 변수임이 명확히 드러났습니다.
>      
> - 3. 반면, **`Look_2`**는 어떤 분석 방식에서도 종속변수와 유의미한 관계를 맺지 못하는 것으로 확인되었습니다.
---

---
## 5. 후진 제거법을 통한 최적 모델 구축 (과제 4-5)

> [!rocket] **단계적 변수 선택 (Backward Method)**
> 유의하지 않은 변수를 하나씩 제거하여 가장 최적화된 모델을 도출했습니다. 

### 모델 구축 과정

1. **1단계**: 모든 변수 포함. 
2. **2단계**: 가장 유의하지 않은 `Look_2`($p=0.763$) 제거. 
3. **3단계**: 다음으로 유의하지 않은 `wmhTotalVol`($p=0.134$) 제거. 

### 최종 모델 결과 (모형 3)

- **최종 선택 변수**: `PeriWMHVol`, `rh_volWM`, `rh_wVolHippocampus`, `Look_1` 
- **최종 모델 적합도**: 수정된 $R^2 = 0.288$, ANOVA $p<.001$ 
- **최종 해석**: 변수를 제거하기 전(0.284)보다 모델의 설명력이 향상(0.288)되었으며, 다중공선성 문제도 완화된 가장 효율적인 모델이 구축되었습니다. 

---
, ',
    displayMath: [['$$', '$$'], ['\\[', '\\]']],
    processEscapes: true,
  },
  svg: {
    fontCache: 'global'
  }
};
</script>
<script type="text/javascript" id="MathJax-script" async
  src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-svg.js">
</script>

>[!QUOTE] 과제 4
>본 노트는 **의학통계학 1차 실습** 과제 4번의 답안에 관한 노트입니다!
>
>수식 및 분석 노트를 원활하게 작성하기 위해 **마크다운 앱**에서 진행했습니다!
>
>PPT 단일로는 **같은 마크다운 문법을 사용하는 생성형 AI와 혼동될 여지**을 고려하여 이렇게 노트의 링크도 첨부했습니다.

---

>[!CHECK] 참여자
>**송채은, 이시연, 정우진**이 본 노트 작성에 참여했습니다!

---

## 1. 다중회귀분석 실시 및 회귀식 도출 (과제 4-1)

> [!info] **분석 설정**
> - **종속변수**: `riskScoreAD` 
> - **설명변수**: `wmhTotalVol`, `PeriWMHVol`, `rh_volWM`, `rh_wVolHippocampus` 
> - **분석 방법**: 입력(Enter) 방식 

### 분석 결과 요약

- **회귀 계수 및 유의성**:
    - `PeriWMHVol` ($B=0.011, p=0.008$), `rh_volWM` ($B=0.000, p<.001$), `rh_wVolHippocampus` ($B=-8.426, p<.001$)는 유의미한 영향력을 보였습니다. 
    - 반면, `wmhTotalVol` ($B=-0.006, p=0.129$)은 통계적으로 유의하지 않았습니다. 

### 회귀식 (Regression Equation)
$$Y = 88.141 - 0.006 \times \text{wmhTotalVol} + 0.011 \times \text{PeriWMHVol} + 0.000 \times \text{rh\_volWM} - 8.426 \times \text{rh\_wVolHippocampus}$$ 


---

## 2. 다중공선성 진단 (과제 4-2)

- **VIF(분산팽창요인) 결과**:
    - `wmhTotalVol`: **30.954** 
    - `PeriWMHVol`: **30.696** 
- **진단**: 일반적으로 VIF가 10 이상이면 다중공선성이 존재한다고 판단합니다.  본 모델에서는 `wmhTotalVol`과 `PeriWMHVol` 간에 매우 높은 공선성이 발견됐습니다.

---

## 3. 모델의 설명력 및 유의성 (과제 4-3)

- **수정된 결정계수 (Adjusted $R^2$)**: **0.284** 
    - 이는 모델에 포함된 변수들이 `riskScoreAD` 변동의 약 28.4%를 설명하고 있음을 의미합니다. 
- **모델 유의성 (ANOVA)**: $F=28.008, p<.001$ 
    - 유의확률이 0.05 미만이므로, 이 회귀 모델은 통계적으로 매우 유의하며 종속변수를 예측하기에 적합합니다. 

---

## 4. 명목변수 가변수 처리 및 분석 (과제 4-4)


### A. 입력 방식 (Enter Method) 분석 결과

> [!info] 입력 방식 (Enter Method)
> 모든 독립변수를 동시에 투입하여 가변수의 개별 기여도를 평가함.

### **통계 결과** 
| 가변수        | 비표준화 계수(B) | 유의확률(p)  | 결과 해석                |
| :--------- | :--------: | :------: | :------------------- |
| **Look_1** |   9.445    | **.050** | 통계적 유의성 **경계선상**에 있음 |
| **Look_2** |   2.736    | **.763** | 통계적으로 유의하지 **않음**    |

### **관계 해석**

- **Look_1과 종속변수의 관계**: `Look_1`의 **p-값이 0.050**으로 나타나, 유의수준 5%에서 
  종속변수인 `riskScoreAD`와 통계적으로 유의한 관련성이 있다기엔 **경계선**에 있습니다. 
  다만, 양(+)의 계수 값을 가짐에 따라 참조 집단에 비해 위험 점수가 다소 높은 경향을 보입니다.

- **Look_2와 종속변수의 관계**: p-값이 0.763으로 매우 높아, `Look_2` 특성은 `riskScoreAD` 점수 변화에 통계적으로 아무런 영향을 미치지 않는 것으로 판단됩니다.

---

### B. 단계 선택 방식 (Stepwise Method) 분석 결과

> [!info] 단계 선택 방식 (Stepwise Method)
> 통계적으로 유의미한 변수만을 선별하여 최적의 모델을 구축함.

### **통계 결과** 
| 가변수 | 비표준화 계수(B) | 유의확률(p) | 결과 해석 |
| :--- | :---: | :---: | :--- |
| **Look_1** | 8.710 | **.026** | **통계적으로 유의함** (최종 모델 포함) |
| **Look_2** | - | - | **제외됨** (유의성 부족) |

### **관계 해석**
- **Look_1과 종속변수의 관계**: 단계 선택법의 최종 모델(4단계)에서 **`Look_1`은 p-값 0.026**으로 통계적으로 **유의한 변수**로 채택되었습니다. 이는 다른 유의한 변수(`rh_wVolHippocampus` 등)들의 영향력을 통제했을 때, **`Look_1` 집단이 참조 집단보다 `riskScoreAD` 점수가 약 8.710점 더 높음**을 의미합니다.
- **Look_2와 종속변수의 관계**: `Look_2`는 변수 선택 과정에서 **유의확률이 낮아** 모델에 진입하지 못하고 제외되었습니다. 이는 `Look_2`가 종속변수를 설명하는 데 통계적 기여도가 없음을 시사합니다.

---

### C. 종합 결론 및 비교

> [!check] **핵심 요약**
> - 1. 본 4-4 과제에선, `LOOK`의 의미를 해석할 수 없어, 의학적으로 **유의미한 변수인지 아닌지**를 알 수 없기 때문에 일반적으로 사용하는 **입력**방식과 통계적인 값을 중점적으로 고려하는 **단계 선택** 방식을 모두 사용했습니다.
>      
> - 2. **입력 방식**에서는 `Look_1`의 유의성이 모호했으나, 불필요한 변수(유의하지 않은 변수들)를 제거한 **단계 선택 방식**에서는 `Look_1`이 종속변수에 유의한 영향을 미치는 핵심 변수임이 명확히 드러났습니다.
>      
> - 3. 반면, **`Look_2`**는 어떤 분석 방식에서도 종속변수와 유의미한 관계를 맺지 못하는 것으로 확인되었습니다.
---

---
## 5. 후진 제거법을 통한 최적 모델 구축 (과제 4-5)

> [!rocket] **단계적 변수 선택 (Backward Method)**
> 유의하지 않은 변수를 하나씩 제거하여 가장 최적화된 모델을 도출했습니다. 

### 모델 구축 과정

1. **1단계**: 모든 변수 포함. 
2. **2단계**: 가장 유의하지 않은 `Look_2`($p=0.763$) 제거. 
3. **3단계**: 다음으로 유의하지 않은 `wmhTotalVol`($p=0.134$) 제거. 

### 최종 모델 결과 (모형 3)

- **최종 선택 변수**: `PeriWMHVol`, `rh_volWM`, `rh_wVolHippocampus`, `Look_1` 
- **최종 모델 적합도**: 수정된 $R^2 = 0.288$, ANOVA $p<.001$ 
- **최종 해석**: 변수를 제거하기 전(0.284)보다 모델의 설명력이 향상(0.288)되었으며, 다중공선성 문제도 완화된 가장 효율적인 모델이 구축되었습니다. 

---
], ['\\(', '\\)']],
    displayMath: [['$$', '$$'], ['\\[', '\\]']],
    processEscapes: true,
  },
  svg: {
    fontCache: 'global'
  }
};
</script>
<script type="text/javascript" id="MathJax-script" async
  src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-svg.js">
</script>

>[!QUOTE] 과제 4
>본 노트는 **의학통계학 1차 실습** 과제 4번의 답안에 관한 노트입니다!
>
>수식 및 분석 노트를 원활하게 작성하기 위해 **마크다운 앱**에서 진행했습니다!
>
>PPT 단일로는 **같은 마크다운 문법을 사용하는 생성형 AI와 혼동될 여지**을 고려하여 이렇게 노트의 링크도 첨부했습니다.

---

>[!CHECK] 참여자
>**송채은, 이시연, 정우진**이 본 노트 작성에 참여했습니다!

---

## 1. 다중회귀분석 실시 및 회귀식 도출 (과제 4-1)

> [!info] **분석 설정**
> - **종속변수**: `riskScoreAD` 
> - **설명변수**: `wmhTotalVol`, `PeriWMHVol`, `rh_volWM`, `rh_wVolHippocampus` 
> - **분석 방법**: 입력(Enter) 방식 

### 분석 결과 요약

- **회귀 계수 및 유의성**:
    - `PeriWMHVol` ($B=0.011, p=0.008$), `rh_volWM` ($B=0.000, p<.001$), `rh_wVolHippocampus` ($B=-8.426, p<.001$)는 유의미한 영향력을 보였습니다. 
    - 반면, `wmhTotalVol` ($B=-0.006, p=0.129$)은 통계적으로 유의하지 않았습니다. 

### 회귀식 (Regression Equation)
$$Y = 88.141 - 0.006 \times \text{wmhTotalVol} + 0.011 \times \text{PeriWMHVol} + 0.000 \times \text{rh\_volWM} - 8.426 \times \text{rh\_wVolHippocampus}$$ 


---

## 2. 다중공선성 진단 (과제 4-2)

- **VIF(분산팽창요인) 결과**:
    - `wmhTotalVol`: **30.954** 
    - `PeriWMHVol`: **30.696** 
- **진단**: 일반적으로 VIF가 10 이상이면 다중공선성이 존재한다고 판단합니다.  본 모델에서는 `wmhTotalVol`과 `PeriWMHVol` 간에 매우 높은 공선성이 발견됐습니다.

---

## 3. 모델의 설명력 및 유의성 (과제 4-3)

- **수정된 결정계수 (Adjusted $R^2$)**: **0.284** 
    - 이는 모델에 포함된 변수들이 `riskScoreAD` 변동의 약 28.4%를 설명하고 있음을 의미합니다. 
- **모델 유의성 (ANOVA)**: $F=28.008, p<.001$ 
    - 유의확률이 0.05 미만이므로, 이 회귀 모델은 통계적으로 매우 유의하며 종속변수를 예측하기에 적합합니다. 

---

## 4. 명목변수 가변수 처리 및 분석 (과제 4-4)


### A. 입력 방식 (Enter Method) 분석 결과

> [!info] 입력 방식 (Enter Method)
> 모든 독립변수를 동시에 투입하여 가변수의 개별 기여도를 평가함.

### **통계 결과** 
| 가변수        | 비표준화 계수(B) | 유의확률(p)  | 결과 해석                |
| :--------- | :--------: | :------: | :------------------- |
| **Look_1** |   9.445    | **.050** | 통계적 유의성 **경계선상**에 있음 |
| **Look_2** |   2.736    | **.763** | 통계적으로 유의하지 **않음**    |

### **관계 해석**

- **Look_1과 종속변수의 관계**: `Look_1`의 **p-값이 0.050**으로 나타나, 유의수준 5%에서 
  종속변수인 `riskScoreAD`와 통계적으로 유의한 관련성이 있다기엔 **경계선**에 있습니다. 
  다만, 양(+)의 계수 값을 가짐에 따라 참조 집단에 비해 위험 점수가 다소 높은 경향을 보입니다.

- **Look_2와 종속변수의 관계**: p-값이 0.763으로 매우 높아, `Look_2` 특성은 `riskScoreAD` 점수 변화에 통계적으로 아무런 영향을 미치지 않는 것으로 판단됩니다.

---

### B. 단계 선택 방식 (Stepwise Method) 분석 결과

> [!info] 단계 선택 방식 (Stepwise Method)
> 통계적으로 유의미한 변수만을 선별하여 최적의 모델을 구축함.

### **통계 결과** 
| 가변수 | 비표준화 계수(B) | 유의확률(p) | 결과 해석 |
| :--- | :---: | :---: | :--- |
| **Look_1** | 8.710 | **.026** | **통계적으로 유의함** (최종 모델 포함) |
| **Look_2** | - | - | **제외됨** (유의성 부족) |

### **관계 해석**
- **Look_1과 종속변수의 관계**: 단계 선택법의 최종 모델(4단계)에서 **`Look_1`은 p-값 0.026**으로 통계적으로 **유의한 변수**로 채택되었습니다. 이는 다른 유의한 변수(`rh_wVolHippocampus` 등)들의 영향력을 통제했을 때, **`Look_1` 집단이 참조 집단보다 `riskScoreAD` 점수가 약 8.710점 더 높음**을 의미합니다.
- **Look_2와 종속변수의 관계**: `Look_2`는 변수 선택 과정에서 **유의확률이 낮아** 모델에 진입하지 못하고 제외되었습니다. 이는 `Look_2`가 종속변수를 설명하는 데 통계적 기여도가 없음을 시사합니다.

---

### C. 종합 결론 및 비교

> [!check] **핵심 요약**
> - 1. 본 4-4 과제에선, `LOOK`의 의미를 해석할 수 없어, 의학적으로 **유의미한 변수인지 아닌지**를 알 수 없기 때문에 일반적으로 사용하는 **입력**방식과 통계적인 값을 중점적으로 고려하는 **단계 선택** 방식을 모두 사용했습니다.
>      
> - 2. **입력 방식**에서는 `Look_1`의 유의성이 모호했으나, 불필요한 변수(유의하지 않은 변수들)를 제거한 **단계 선택 방식**에서는 `Look_1`이 종속변수에 유의한 영향을 미치는 핵심 변수임이 명확히 드러났습니다.
>      
> - 3. 반면, **`Look_2`**는 어떤 분석 방식에서도 종속변수와 유의미한 관계를 맺지 못하는 것으로 확인되었습니다.
---

---
## 5. 후진 제거법을 통한 최적 모델 구축 (과제 4-5)

> [!rocket] **단계적 변수 선택 (Backward Method)**
> 유의하지 않은 변수를 하나씩 제거하여 가장 최적화된 모델을 도출했습니다. 

### 모델 구축 과정

1. **1단계**: 모든 변수 포함. 
2. **2단계**: 가장 유의하지 않은 `Look_2`($p=0.763$) 제거. 
3. **3단계**: 다음으로 유의하지 않은 `wmhTotalVol`($p=0.134$) 제거. 

### 최종 모델 결과 (모형 3)

- **최종 선택 변수**: `PeriWMHVol`, `rh_volWM`, `rh_wVolHippocampus`, `Look_1` 
- **최종 모델 적합도**: 수정된 $R^2 = 0.288$, ANOVA $p<.001$ 
- **최종 해석**: 변수를 제거하기 전(0.284)보다 모델의 설명력이 향상(0.288)되었으며, 다중공선성 문제도 완화된 가장 효율적인 모델이 구축되었습니다. 

---
